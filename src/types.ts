export type Cards = {
  id: string
  tags: string[]
  image: string
  url: string
  title: string
  description: string
  featured: number
}

export type Filters = {
  tag: string
  checked: boolean
}
